name: Build and Release

on:
  push:
    # 当推送带标签的提交时触发（格式：v*.*.*）
    tags:
      - 'v*.*.*'
  workflow_dispatch:  # 允许手动触发

jobs:
  build-windows:
    name: Build on Windows
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.8'

    - name: Cache pip dependencies
      uses: actions/cache@v4
      with:
        path: ~\AppData\Local\pip\Cache
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt', '**/pyproject.toml') }}
        restore-keys: |
          ${{ runner.os }}-pip-

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller

    - name: Build executable
      run: |
        # 清理之前的构建文件
        Write-Host "清理构建目录..." -ForegroundColor Yellow
        if (Test-Path "build") { Remove-Item -Recurse -Force build }
        if (Test-Path "dist") { Remove-Item -Recurse -Force dist }
        if (Test-Path "release") { Remove-Item -Recurse -Force release }

        # 检查源文件是否存在
        Write-Host ""
        Write-Host "检查源文件..." -ForegroundColor Yellow
        if (Test-Path ".env.example") {
          Write-Host "  ✓ 找到 .env.example" -ForegroundColor Green
        } else {
          Write-Host "  ✗ 未找到 .env.example" -ForegroundColor Red
        }
        if (Test-Path ".env") {
          Write-Host "  ✓ 找到 .env" -ForegroundColor Green
        } else {
          Write-Host "  ✗ 未找到 .env" -ForegroundColor Red
        }

        # 使用PyInstaller构建
        Write-Host ""
        Write-Host "开始构建可执行文件..." -ForegroundColor Yellow
        pyinstaller build.spec --clean

        # 准备发布文件
        Write-Host ""
        Write-Host "准备发布文件..." -ForegroundColor Yellow
        mkdir release -Force

        # 复制exe文件
        Write-Host "  复制 mini-open2anth.exe..." -ForegroundColor Cyan
        Copy-Item "dist\mini-open2anth.exe" "release\mini-open2anth.exe" -Force

        # 复制配置文件
        if (Test-Path ".env.example") {
          Write-Host "  复制 .env.example..." -ForegroundColor Cyan
          Copy-Item ".env.example" "release\.env.example" -Force
        }
        if (Test-Path ".env") {
          Write-Host "  复制 .env..." -ForegroundColor Cyan
          Copy-Item ".env" "release\.env" -Force
        }

        # 显示最终结果
        Write-Host ""
        Write-Host "发布目录内容：" -ForegroundColor Green
        Get-ChildItem release | Format-Table Name, Length, LastWriteTime -AutoSize

    - name: Get version
      id: version
      run: |
        # 从 pyproject.toml 提取版本号
        $content = Get-Content pyproject.toml -Raw
        if ($content -match 'version\s*=\s*"([^"]+)"') {
          $version = $matches[1]
        } else {
          $version = "0.1.0"
        }
        echo "VERSION=$version" >> $env:GITHUB_OUTPUT
        echo "Version extracted: $version"

    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: mini-open2anth-windows-${{ steps.version.outputs.VERSION }}
        path: release/
        retention-days: 30

  create-release:
    name: Create GitHub Release
    needs: build-windows
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Get version from tag
      id: tag
      run: |
        # 从标签中提取版本号（去掉 'v' 前缀）
        VERSION=${GITHUB_REF#refs/tags/v}
        echo "VERSION=$VERSION" >> $GITHUB_OUTPUT
        echo "Version: $VERSION"

    - name: Download artifacts
      uses: actions/download-artifact@v4
      with:
        name: mini-open2anth-windows-${{ steps.tag.outputs.VERSION }}
        path: release/
        include-hidden-files: true

    - name: Create Release
      uses: softprops/action-gh-release@v2
      with:
        tag_name: ${{ github.ref_name }}
        name: Release ${{ steps.tag.outputs.VERSION }}
        body: |
          ## Changes in this Release

          🎉 This is an automated release containing the Windows executable for **mini-open2anth**.

          ### 📦 What's Included

          - `mini-open2anth.exe` - Windows executable file

          ### 🚀 How to Use

          1. Download and run `mini-open2anth.exe`
          2. Make sure you have a `.env` configuration file in the same directory
          3. Or copy `.env.example` to `.env` and modify the configuration

          ### 📝 Notes

          - This is a single-file executable with all dependencies included
          - Built with PyInstaller on Windows

          ---
          **Built with ❤️ by GitHub Actions**

        files: |
          release/mini-open2anth.exe
          release/.env.example
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
